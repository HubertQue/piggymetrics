spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config-6z65pczetq-uc.a.run.app:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
