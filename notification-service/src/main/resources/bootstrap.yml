spring:
  application:
    name: notification-service
  cloud:
    config:
      uri: http://config-6z65pczetq-uc.a.run.app
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
