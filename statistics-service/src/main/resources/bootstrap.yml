spring:
  application:
    name: statistics-service
  cloud:
    config:
      uri: https://config-6z65pczetq-uc.a.run.app
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
